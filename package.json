{
  "name": "aws-whats-new-korean",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:check": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy:lambda": "cd aws-lambda/fetch-updates && npm install && zip -r ../fetch-updates.zip . && aws lambda update-function-code --function-name aws-whats-new-fetch-updates --zip-file fileb://../fetch-updates.zip",
    "deploy:infrastructure": "aws cloudformation deploy --template-file aws-infrastructure/cloudformation.yaml --stack-name aws-whats-new-infrastructure --capabilities CAPABILITY_NAMED_IAM --parameter-overrides Environment=prod"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^4.16.16",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^5.1.2",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15"
  }
}
